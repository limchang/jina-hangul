/**
 * @typedef {{ id: number, char: string, x: number, y: number, scale: number, done: boolean, groupId?: number, word?: string }} Piece
 */

/**
 * @typedef {{ path: string }} Stroke
 */

/**
 * @typedef {{ char: string, strokes: Stroke[] }} Source
 */

/**
 * @typedef {{ a: number, b: number, answer: number, options: number[] }} MathQuiz
 */

/**
 * @typedef {'easy' | 'normal' | 'hard'} Difficulty
 */

/**
 * @typedef {{ x: number, y: number }} Point
 */

/**
 * @typedef {{ minX: number, maxX: number, minY: number, maxY: number, w: number, h: number }} BBox
 */

export {};
